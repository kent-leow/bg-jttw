import { describe, it } from "vitest";

describe.skip("usePlayerGameState", () => {
  // SKIPPED: Tests use deleted connection and crypto modules (RoomHub, HostOrchestrator)
  // Will be replaced by useLocalGameState.spec.ts in task 005
  it("placeholder", () => {});
});

