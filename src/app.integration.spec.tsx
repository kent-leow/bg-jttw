import { describe, it } from "vitest";

/**
 * Integration tests for the multi-device WebRTC/QR flow have been removed.
 * These tests will be replaced in task-002 with tests for the new single-device
 * pass-and-play architecture.
 */
describe.skip("App integration (multi-device flow)", () => {
  it("placeholder - integration tests removed in task-001", () => {});
});
